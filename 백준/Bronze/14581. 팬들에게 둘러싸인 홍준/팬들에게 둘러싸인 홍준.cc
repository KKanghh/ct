#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	string name;
	cin >> name;

	cout << ":fan::fan::fan:\n:fan::" << name << "::fan:\n:fan::fan::fan:";
}